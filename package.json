{
  "name": "clipcontext",
  "version": "1.0.0",
  "description": "AI-powered video-to-social-metadata pipeline (FastAPI backend + Next.js frontend). This is a Python/Node monorepo — see the Makefile for the canonical dev commands (`make setup`, `make backend`, `make frontend`, `make test`); this file exists only so editors/tools that expect a root package.json find one.",
  "private": true,
  "keywords": [],
  "author": "",
  "license": "MIT"
}
